<%= form_for(@it_park) do |f| %>
  <% if @it_park.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@it_park.errors.count, "error") %> prohibited this it_park from being saved:</h2>

      <ul>
      <% @it_park.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.cktext_area :content, :ckeditor => {:uiColor => '#AADC6E'} %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
