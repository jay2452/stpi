<%= simple_form_for(@extra) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.cktext_area :content, :ckeditor => {:uiColor => '#AADC6E'} %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
