<%= form_for(@gcell) do |f| %>
  <% if @gcell.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gcell.errors.count, "error") %> prohibited this gcell from being saved:</h2>

      <ul>
      <% @gcell.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
  <%= f.cktext_area :content, :ckeditor => {:uiColor => '#AADC6E'} %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
