<div class="container-fluid">
  <div class="col-md-3">

  </div>

  <div class="col-md-6">
    <div class="table-responsive">
       <table class="table table-hover">
         <thead>
           <h4 align="center">News and events (<%= link_to "Add", new_feed_path %>)</h4>
         </thead>
         <tbody>
           <% @feeds.each do |feed| %>
             <tr>
               <td><%= raw feed.topic %></td>
               <td> </td>
             </tr>
           <% end %>
         </tbody>
       </table>
    </div>
  </div>

  <div class="col-md-3">

  </div>
</div>
